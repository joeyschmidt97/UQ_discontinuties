numpy>=1.26,<2
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.6
threadpoolctl>=3.1
pytest>=7

# Display-only projections for the high-dimensional embedding sheets
# (scripts/embed_6d.py). Nothing is computed in the projected space.
umap-learn>=0.5
pacmap>=0.7

# Optional real SG++ backend: install pysgpp separately in a compatible environment.
# sg_lib: set SG_LIB_PATH to its external checkout; it is not vendored here.
